const obj = {100: 'a', 2: 'b', 7:'c'};

const keys = Object.keys(obj); // Vai criar um array do tipo 'Number'.
const values = Object.values(obj); // Vai criar um array do tipo 'String'.

console.log(values);